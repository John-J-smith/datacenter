# 参变量块由主机程序按页装箱并覆盖生成头文件

参变量不再手写块号。构建时用主机 C 程序按清单顺序装箱：存储特性不同或超过 128 字节上限（含 2 字节校验、对齐 EEPROM 一页）则新开一块。链接数组独占连续页，整条记录不跨页；单条记录仍超载荷则生成失败。默认值在侧车文件，生成的 `dc_param_layout.h` 放在源码树中入库，每次构建覆盖。不在运行时装箱，也不把布局只放在构建目录。不改用 Python 生成器。
